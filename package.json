{
  "name": "omnivid",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "vercel-build": "node vercel-build.js"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}