{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "frontend/.next/**",
        "frontend/.next/static/**"
      ]
    },
    "dev": {
      "cache": false
    },
    "start": {
      "dependsOn": ["build"]
    },
    "lint": {}
  }
}